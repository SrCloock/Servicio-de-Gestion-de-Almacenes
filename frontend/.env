# ============================================================
# CONFIGURACIÓN DE ENTORNO — FRONTEND (Vite)
# Copia este archivo a frontend/.env y ajusta la IP/puerto.
# ¡No subas .env a git!
# ============================================================

# URL completa del backend.
# En producción: IP pública + puerto del backend.
# En desarrollo con Vite: puedes dejarlo vacío → usa window.location.origin
VITE_API_URL=http://80.24.244.68:3000

VITE_API_URL=http://localhost:3000

# Solo informativo (no usado en código, útil para depuración)
VITE_APP_ENV=production
